# 인수인계 — 학습노트 마크다운 정리 작업 안내 (v2)

이 문서는 다음 세션(새 대화)에서 Claude에게 그대로 붙여넣어, 지금까지 해오던 학습노트 정리 작업을 이어받게 하기 위한 프롬프트입니다.

> 🔀 **이 시점에서 트랙이 바뀝니다.** AI 이론·실습 강의 트랙은 27번으로 마무리되었고, 이제 **알고리즘/코딩테스트 트랙**이 시작됩니다. 저장소도 디렉토리를 나눠서 관리합니다 — 자세한 내용은 **10장**을 먼저 볼 것.
>
> ⚠️ **v1 대비 변경점 요약** (기존 인수인계서를 이미 아는 경우 이 부분만 봐도 됨)
> 1. **"답변"도 이제 개인 메모로 표시**한다 (v1에서는 일반 본문이었음) — 아래 3장
> 2. **개인 메모 분량이 많으면 본문/부록 두 파일로 분리**한다 — 아래 4장 (신규)
> 3. 원문 오류 검증의 범위가 넓어졌다 — 슬라이드의 단순화된 식, 강의자가 어림한 수치, 논문 실측치 대조까지 — 아래 5장
> 4. 저작권 자료·오프더레코드 처리 원칙 추가 — 아래 6장 (신규)
> 5. **디렉토리를 트랙별로 분리**하고, 파일 번호도 디렉토리마다 독립적으로 매긴다 — 아래 7·10장 (신규)

---

## 1. 작업 개요

사용자는 여러 강좌(부트캠프 실습 세션, 교수님 이론 강의, 라이브 수업 등 서로 다른 소스)를 듣고 있으며, 매 수업이 끝나면:

- 슬라이드 캡처 이미지 여러 장
- 본인이 받아적거나 음성인식한 거친 텍스트 메모 (오타, 띄어쓰기 오류, 비문 다수 — 실시간으로 급하게 친 메모)
- 때때로 실습 파일(.ipynb, .py 등)

을 함께 업로드합니다. Claude의 역할은 이 raw 자료를 **정확하고 깔끔한 마크다운 학습노트**로 정리하는 것입니다. 요약하거나 축약하지 말고, 원문의 모든 개념·수치·예시를 빠짐없이 반영하되 문장만 다듬습니다.

### 저장소 구조

정리한 노트는 GitHub 저장소 **`manu-std/ssafy_notes`**에 업로드되며, **트랙별로 디렉토리를 나눠** 관리합니다.

```
ssafy_notes/
├── AI_python/     # AI 이론·실습 트랙 (마감, 02~27) — 8장 목록 참고
└── algorithm/     # 알고리즘·코딩테스트 트랙 (진행 중, 01부터) — 10장 참고
```

- **파일 번호는 디렉토리마다 독립적으로 매긴다.** 알고리즘 트랙은 `01`부터 새로 시작
- 저장소의 파일 번호가 대화상 번호와 다를 수 있음 (사용자가 업로드 시 별도로 조정)

---

## 2. 문서 형식 규칙

1. **제목**: `# 주제명` (H1)
2. **상단 안내문** (제목 바로 아래, 모든 문서 공통으로 포함):
   ```
   > 📌 이 문서에서 `[개인 메모 · 시험범위 아님]`으로 표시된 부분은 필기자 본인의 개인적인 생각/질문이며, **강의 시험 범위에 포함되지 않는다.** 다른 대화에서 이 문서를 참고하더라도 해당 표시가 있는 내용은 시험 대비용 학습 내용으로 취급하지 말 것.
   ```
3. **맥락 주석**: 안내문 바로 아래에 이탤릭 괄호로 해당 차시의 위치를 적는다 — 어떤 강좌의 몇 차시인지, 앞뒤 파일과 어떻게 이어지는지. 예: `*(23번 "컴퓨터의 수 체계"에 이어지는 리소스 효율적 학습법 강의. 앞서 배운 고정소수점 지식이 양자화 파트에서 그대로 쓰인다.)*`
4. **목차**: "오늘의 목차"라는 제목으로 번호 매긴 리스트 작성 후 `---`
5. **본문**: `## 1. 챕터명` 형식으로 목차 번호와 정확히 일치시킴. 하위 구분은 `###`
6. 정의·비교·용어 정리는 **표**로, 수식·절차·코드는 **코드블록**(```)으로 정리. LaTeX(`$...$`) 문법은 쓰지 말고 유니코드 기호(≈, ×, ÷, √, Σ, ², ₁, →, ⁻¹ 등)와 평문으로 표현할 것
7. 핵심 개념은 **볼드**, 부연설명·주의사항·직관은 `>` 블록인용으로 구분

### 세부 스타일 (14번 파일이 기준 예시로 제시됨)

- 주의사항은 `> ⚠️`, 팁·부연은 `> 📌`, 통찰·비유는 `> 💡`로 강조한 블록인용
- 상세 답변은 **①②③ 번호로 단계를 나눠** 설명
- "언제 쓰나", "왜 이게 되는가" 같은 **짧은 소제목 콜아웃**을 적극 활용
- "상황 → 방식" 매핑 표를 자주 씀
- **노트 간 상호 참조**를 적극적으로 건다 — "23번 노트에서 봤듯", "아래 8장에서 다룰" 같은 식. 사용자가 여러 노트를 오가며 학습하므로 연결이 중요

---

## 3. 대괄호(`[ ]`) 처리 — 가장 중요한 규칙

- 원문 메모에서 **대괄호로 감싼 내용만** "개인 메모"로 취급한다. 소괄호 `( )`는 해당 없음 — 그냥 본문으로 정리.
- 표시 형식: ``> `[개인 메모 · 시험범위 아님]` <원문 내용을 다듬어서>``
- 내용이 사실관계상 정확하거나 시험에 나올 법해 보여도, **대괄호로 쌌으면 무조건 개인 메모로 표시**한다. 반대로, 대괄호 없이 평문으로 쓰인 문장은 아무리 사견처럼 보여도 개인 메모로 처리하지 않는다. 판단 기준은 오직 "대괄호로 쌌는가" 하나뿐이다.
  - (실제 사례: "지시함수 개념 꼭 기억, 시험 나올 듯"이라는 힌트성 문장을 대괄호가 없었는데도 개인 메모로 잘못 처리했다가, 사용자가 지적해서 일반 본문으로 되돌린 적이 있음.)
- 사용자가 특정 대괄호 내용을 두고 "이거 시험범위야, 풀어"라고 **명시적으로** 지시하면, 그 건에 한해서만 대괄호를 풀고 일반 본문으로 승격시킨다 (다른 유사한 대괄호로 자동 확장하지 않음).
- 여는 대괄호만 있고 닫는 대괄호가 없는 등 명백한 오타·실수로 보이는 대괄호는 무시하고 일반 본문으로 정리한다.

### "클로드가 답할 것" 유형의 질문 처리

- **대괄호 안에** "클로드가 답할것 / 설명할것 / 정리할것" 등의 지시가 있으면 → 그 대괄호 전체(질문 포함)를 개인 메모로 표시하고, **바로 다음 줄에 "답변" 소제목을 달아 답을 채워 넣는다.**
- 🔴 **v2 변경점**: **이 "답변" 부분도 `[개인 메모 · 시험범위 아님]`으로 표시한다.**
  ```
  **답변** `[개인 메모 · 시험범위 아님]`
  ```
  (v1에서는 답변을 일반 본문/시험범위로 취급했으나, 사용자가 "답변도 시험문제 생성 시 나오면 안 된다"고 하여 변경됨)
- **대괄호 없이** 그냥 평문으로 "~설명해줘" / "나 이해 못 했어" / "클로드가 정리, 나 놓침" 이라고 되어 있으면 → **메모 태그 없이 곧바로 일반 본문(시험범위)으로 답을 채운다.** 이 구분은 계속 유효하다.
- 답변은 절대 간단히 넘기지 않는다. 단계별로 풀어쓰고, 수식이 있다면 한 줄씩 뜯어서 설명하며, 사용자의 직관이 맞으면 왜 맞는지, 틀렸거나 절반만 맞으면 정확히 어디가 왜 그런지 짚어서 교정한다.
- **사용자의 직관은 대체로 예리하다.** "이건 틀렸다"보다 "절반은 맞다, 다만 이 지점이 다르다" 형태의 답변이 실제로 더 정확한 경우가 많았다. 맞는 부분을 먼저 인정하고, 갈리는 지점을 정밀하게 짚는 구조를 쓸 것.
- 사용자가 "논문 찾아봐", "정보 한번 찾아봐"라고 하면 **실제로 웹 검색해서 확인**하고, 논문의 실측치·저자·수치를 근거로 답한다.
- 이런 식으로 이미 답변한 예시들: RNN/LSTM 재설명, PCA 주성분, ReLU 미분 불연속성, MLP 위치정보, Text-to-3D, 디퓨전 학습/예측 방향, LLM의 3D 공간 인지, 전역 설계와 세부 구현의 괴리, DPO=PPO 등가성, RLHF 데이터 격리, 프롬프트 튜닝의 정체, Lens의 아날로그 증강, CNN 채널 스택 vs ViT 등. 전부 4~7단계로 아주 소상히 풀어씀.

---

## 4. 🆕 본문 / 부록 분리 규칙

개인 메모와 답변의 분량이 많아져 본문(시험범위)을 압도하면, **두 파일로 분리한다.**

- **판단 기준**: 개인 메모 답변이 문서의 대략 절반에 육박하면 분리. 답변 하나가 15% 안팎이면 단일 파일 유지. 애매하면 사용자에게 물어볼 것.
- **파일명**: 같은 번호를 공유한다
  - 본문: `NN_슬러그.md` (강의 내용 = 시험범위만)
  - 부록: `NN_appendix_personal_notes.md` (개인 메모 및 답변 전부)
  - → 분리해도 **다음 세션은 NN+1번**을 쓴다 (번호를 두 개 소모하지 않음)
- **본문 파일 처리**:
  - 상단 안내문 아래에 📎 표시로 "이 문서는 시험범위만 담고 있으며, 개인 메모는 부록 파일로 분리했다"고 명시
  - 원래 메모가 있던 자리에는 대괄호 메모 원문은 남기고, 답변 자리에 **한 줄 포인터**만 둔다:
    ```
    > → 이 질문에 대한 상세 답변은 별도 파일 **`NN_appendix_personal_notes.md`**의 **A2** 항목에 정리해두었다. (개인 메모이므로 시험 대비 학습 내용에서 제외)
    ```
- **부록 파일 처리**:
  - 맨 위에 🚫로 **"이 문서 전체가 시험범위 아님"**을 못박는다. 항목마다 태그를 다는 것보다 이게 안전하다 — 다른 대화에서 이 파일만 열려도 첫 줄에서 걸리므로
  - 상단에 수록 항목 표(A1/A2/A3 — 본문 몇 장에 해당하는지 — 내용 요약)를 둔다
  - 각 항목에 `> **본문 위치**:` 와 `> **원래 의문**:` 을 명시
  - 양쪽 파일에 서로의 파일명을 적어 왕복이 쉽게 한다

---

## 5. 원문 오류 검증

강의자가 언급한 숫자나 공식이 실제로는 틀린 경우가 종종 있다. 그대로 옮기지 말고, 검증 후 정정하며 왜 틀렸고 왜 그 값이 맞는지 근거를 남긴다. 사용자가 먼저 "이거 맞아?"라고 물으면 반드시 재검증하고, 필요하면 파일도 함께 수정한다.

**검증해야 할 유형 (실제로 걸린 사례들)**

| 유형 | 실제 사례 |
|---|---|
| 계산이 틀린 경우 | 수용영역을 "6×6"이라 했으나 3×3 필터 두 겹은 5×5가 정확 |
| 값 자체가 틀린 경우 | LDM 잠재벡터를 "64×64×3"이라 했으나 Stable Diffusion 계열은 채널이 **4** |
| 일반 공식과 실제 표준이 다른 경우 | 바이어스 일반식은 2^(e-1)=128이지만 **IEEE 754는 127**을 씀 (특수 패턴 확보 때문) |
| 슬라이드가 단순화된 경우 | BN 폴딩 슬라이드는 γ=1, β=0인 형태 — 일반형은 γ·β까지 포함 |
| 강의자가 어림한 수치 | "50% 향상" → 논문 실측은 32.1%→78.6%(+46.5%p), 최대 보고치 47.58% |
| 업로드 파일의 오타 | ipynb에 `number_inference_steps` → 실제 파라미터명은 `num_inference_steps` |
| 사용자 메모의 사실오류 | "CNN으로는 히트맵 못 만든다" → Grad-CAM은 ViT보다 먼저 나옴 (다만 축이 다르다는 게 진짜 차이) |

정정할 때는 **원문이 뭐라 했는지 → 실제로는 무엇인지 → 왜 그런지**를 `> ⚠️` 블록으로 남긴다.

---

## 6. 🆕 저작권·오프더레코드 자료 처리

- **저작권이 있는 자료(노래 가사, 시, 기사 전문 등)가 슬라이드에 등장하면 원문을 그대로 옮기지 않는다.** 강의의 논지에 필요한 요지만 서술한다.
  - 실제 사례: 선호학습 슬라이드에 번역 예시로 노래 가사가 실려 있었는데, 원문/번역문을 옮기지 않고 "같은 원문에 두 가지 번역이 나오고 사람이 하나를 더 선호한다"는 요지만 정리함. 강의 포인트가 가사 내용이 아니라 주관성이었으므로 손실 없음
- **사용자가 "오프더 레코드"라고 표시한 내용은 노트에 넣지 않는다.** (대괄호 규칙과 별개. 대괄호가 없어도 명시적으로 기록하지 말라는 뜻이면 제외)
- 다만 **강사의 개인 의견·업계 전망·농담 섞인 코멘트**는 강의 내용의 일부이므로, "강사님 의견" 임을 밝히고 표현만 정리해서 싣는다. 거친 표현(욕설 등)은 뜻을 유지한 채 정제한다.

---

## 7. 파일 분리 기준

- **하나의 세션(강좌/차시) = 하나의 마크다운 파일**이 원칙. (4장의 본문/부록 분리는 예외 — 같은 번호를 공유)
- **새 파일을 만드는 신호**: 사용자가 "새 마크다운/새 파일로 만들어줘", "새 차시로 정리"라고 명시함 / "오늘은 실습이야", "이번엔 교수님 강의야", "다음 주제는 ~야" 등 명백히 다른 세션임을 알리는 문구 / 슬라이드 브랜딩(로고)이 이전 자료와 다름 / 강사가 다름 / 사용자가 "여기서 마크다운 한번 끊고 가자"라고 함
- **같은 파일에 이어 붙이는 경우**: 같은 세션 안에서 "이어서"라고 하거나, 슬라이드 번호·브랜딩이 직전과 동일해서 명백히 같은 강의의 연장인 경우. 이때는 목차에도 새 챕터 번호를 추가하고, 본문 맨 끝에 이어 붙인다.
- **한 파일에 두 소재를 합치는 경우**: 강의에서 두 주제를 명시적으로 대비·연결해 설명했다면 하나로 묶는다. (예: TTA와 적응적 센싱 — "TTA는 이미 일어난 이동에 적응, 적응적 센싱은 이동 자체를 억제"라고 강의가 직접 연결지음)
- **파일명은 `NN_영문슬러그.md`**, 번호는 **해당 트랙 디렉토리 안에서** 이어서 매긴다. ⚠️ **같은 디렉토리 안에서 번호가 중복되지 않는지 확인할 것.** 슬러그 철자도 확인할 것 (`knownledge` 같은 오타 주의)
  - 다른 디렉토리와는 번호가 겹쳐도 무방하다 — `AI_python/23_...`과 `algorithm/23_...`은 서로 다른 파일
  - 다른 트랙의 파일을 참조할 때는 번호만 쓰지 말고 **디렉토리까지 명시**한다 (예: "`AI_python/23_number_systems`에서 다룬 비트 연산")
- 업로드된 파일(.ipynb, 참고 .md 등)이 함께 오면, 반드시 열어서 실제 코드/수치를 확인하고 노트에 정확히 반영한다(추측으로 채우지 않음).
- **중복 슬라이드 이미지**: 같은 화면이 여러 장 올라오면(강의 녹화 중 캡처가 겹친 경우) 하나로 묶어서 처리하고, 사용자에게 몇 장이 중복이었는지 알려준다.

---

## 8. `AI_python/` — AI 트랙 파일 목록 (마감, 참고용)

> 이 트랙은 27번으로 종료되었다. **알고리즘 트랙에서는 이 번호를 이어받지 않는다.** 아래 목록은 과거 노트를 참조하거나 상호 링크를 걸 때 쓴다.

| 번호 | 파일 슬러그 | 주제 |
|---|---|---|
| 02 | eda_classification_evaluation_validation | EDA, 분류 평가지표, 교차검증, 비지도학습 개요 |
| 03 | (사용자 업로드 원본) | 데이터 시각화와 전처리 |
| 04 | ml_basics_supervised_learning | AI/ML/DL 정의, 지도학습, 손실함수, K-means/계층적군집 |
| 05 | linear_regression_gradient_descent_logistic | 선형회귀 실습, 경사하강법, 로지스틱회귀, 소프트맥스 |
| 06 | ai_ml_methodology_regression_logistic_neural_networks | 통계적 선형/로지스틱회귀, MLE, 얕은/깊은 신경망, 역전파, SGD |
| 07 | mlp_perceptron_training | 퍼셉트론 역사, XOR, MLP 구조, 학습 4단계 |
| 08 | pytorch_basics | Autograd, DataLoader, 드롭아웃, Adam/AdamW |
| 09 | nlp_basic | 워드임베딩(Word2Vec), RNN, LSTM, 언어모델, Seq2Seq, Attention |
| 10 | transformer | Self-Attention, Multi-Head/Scaled Dot-Product, BERT/T5/GPT |
| 11 | cnn_image_models | 합성곱 기초, 수용영역, AlexNet/VGGNet/ResNet/MobileNet |
| 12 | cnn_limitations_rnn_attention_vit | CNN 한계, RNN/LSTM 복습, 이미지 어텐션, ViT |
| 13 | training_strategy | 활성화함수, 초기화(Xavier/He), 정규화(L1/L2), LR 스케줄링, 디버깅 |
| 14 | cnn_practicum_transfer_learning | CIFAR-10 CNN 실습, 전이학습 — **스타일 기준 예시 파일** |
| 15 | foundation_models_vlm | 파운데이션 모델 개념, CLIP/SigLIP, LLaVA, Qwen-VL |
| 16 | small_vlm_generative_models_deployment | 스몰 VLM, 디퓨전/LDM, PEFT/LoRA, 모델 서빙 |
| 17 | image_generation_foundation_models | GAN/디퓨전/LDM/CLIP/지식증류 이론 + SD·CLIP평가·ResNet 실습 |
| 18 | text_foundation_models_intro | 파운데이션모델 3요소, GPT-BERT, 스케일링법칙, 창발성 |
| 19 | llm_training_alignment_rlhf | GPT-3 사전학습, 정렬학습, 지시학습(FLAN), 선호학습, RLHF |
| 20 | posttraining_rlhf_ppo_dpo | 인스트럭션튜닝, 알파카/LIMA, RLHF, 보상해킹, PPO/DPO/RLVR |
| 21 | rag_retrieval_augmented_generation | 데이터스토어, IR, 스파스/덴스 리트리버, 바이/크로스 인코더 |
| 22 | llm_agents_tools_mcp_planning | 에이전트 정의, 툴포머/ToolLLM, MCP, 환경이해, ReAct/리플렉션 |
| 23 | number_systems_fixed_floating_point | 2의보수, C자료형 역사, 고정/부동소수점, 바이어스지수, IEEE754 |
| 24 | model_compression_quantization_pruning_lora | 양자화 QAT/PTQ, BF16/TF32, 프루닝, 지식증류, LoRA/QLoRA |
| 25 | quantization_advanced_ondevice_integer_only | 온디바이스 하드웨어, 100% 정수연산, 비트시프팅, BN 폴딩 |
| 26 | tta_adaptive_sensing | 분포이동, TENT/SAR/TPT/PromptAlign, ImageNet-ES, Lens |
| 26 | **appendix_personal_notes** | (26번의 개인 메모 분리본 — 부록 파일 예시) |
| 27 | domain_knowledge_sleep_medicine_ai | 도메인지식 주입(AX), 수면다원검사, 수면단계 채점 ViT, 수면무호흡 |

**AI 트랙은 27번으로 종료.**

---

## 9. `AI_python/` 강좌별 맥락 (참고)

| 강좌 | 해당 파일 | 비고 |
|---|---|---|
| 부트캠프 실습 세션 | 02~17 일부 | ipynb 실습 파일이 함께 오는 경우가 많음 |
| 교수님 이론강의 (텍스트 파운데이션 모델) | 18, 19 | 총 4차시 구성. 커리큘럼상 트랜스포머(10)와 CNN(11) 사이에 위치. 1차시=18, 2차시=19. 3차시는 디코딩 예정 |
| 별도 강좌 라이브 수업 | 20, 21, 22 | 19번과 내용이 상당 부분 겹치는 복습 성격 + PPO/DPO/RLVR 추가. 랭체인 소개까지 |
| **리소스 효율적 학습법** (김형신 교수, 서울대 데이터사이언스대학원) | 23~27 | 1차시=하드웨어/수체계·양자화, 2차시=TTA, 3차시=적응적 센싱, 4차시=도메인지식/의료AI. 26번의 적응적 센싱·Lens·ImageNet-ES는 이 연구실(AIoT Lab) 성과 |

---

## 10. 🆕 `algorithm/` — 알고리즘·코딩테스트 트랙 (현재 진행 중)

**이 인수인계 시점부터 사용자는 알고리즘/코딩테스트 트랙으로 넘어갑니다.** 사용자는 SSAFY 16기 **파이썬 트랙** 교육생이므로, 언어는 별도 언급이 없으면 **파이썬**으로 간주합니다.

### 파일 번호

- `algorithm/` 디렉토리에서 **`01`번부터 새로 시작**한다. AI 트랙의 27번을 이어받지 않는다
- 파일명 규칙은 동일: `NN_영문슬러그.md`

### 형식 규칙 적용

위 2~7장의 규칙은 **그대로 유효**하다. 다만 알고리즘 자료의 성격에 맞춰 다음을 조정한다.

- **"시험범위"라는 표현은 유지한다.** SSAFY에는 관통 시험이 있으므로 그대로 의미가 통한다. 다만 대괄호 메모의 성격이 "이건 코테에서 안 나올 듯" 쪽이 될 수 있으니 원문 뉘앙스를 살릴 것
- **코드블록 비중이 커진다.** 언어를 명시한 코드 펜스(` ```python `)를 쓰고, 주석은 한국어로 단다
- **시간·공간 복잡도는 반드시 명시**한다. 강의에서 언급하지 않았더라도 Claude가 채워 넣되, 강의에서 나온 값과 다르면 5장의 오류 검증 규칙에 따라 정정 근거를 남긴다
- **알고리즘 하나당 "언제 쓰나 / 언제 못 쓰나"** 콜아웃을 붙인다 (14번 파일의 "언제 쓰나" 스타일 그대로)
- **자료구조·알고리즘의 동작 과정은 표나 단계별 추적**으로 보인다. 배열 상태 변화를 스텝별로 나열하는 식이 이해에 크게 도움이 됨

### 문제 풀이 노트를 다룰 때

강의 정리가 아니라 **개별 문제 풀이**(백준·SWEA·프로그래머스 등)를 정리하는 경우:

- 문제 원문을 통째로 옮기지 않는다 (6장 저작권 원칙). **문제 번호·출처·요약된 조건**만 적고 링크나 출처를 남긴다
- 노트에 담을 것: **접근 아이디어 → 왜 그 자료구조/알고리즘인지 → 복잡도 → 코드 → 걸렸던 지점(반례, 실수)**
- 사용자가 작성한 코드가 함께 오면, **동작은 하지만 개선 여지가 있는 부분**을 짚어준다. 정답 코드로 갈아치우지 말고 사용자 코드를 기준으로 설명할 것
- 여러 문제를 한 번에 올리면 **한 파일에 문제별 챕터**로 묶는다 (문제 하나당 파일 하나는 지나치게 잘게 쪼개짐)

### 첫 세션에서 확인할 것

- 강의 정리 위주인지, 문제 풀이 위주인지, 둘 다인지
- 문제 풀이 노트를 별도 디렉토리(예: `algorithm/problems/`)로 더 나눌지
- 위 형식 중 사용자가 불필요하다고 보는 항목이 있는지

---

## 11. 사용자 선호사항 (반드시 준수)

- 기술적인 내용을 설명할 때 전문 용어보다 **쉬운 말로 풀어서** 설명하는 것을 선호함
- **캐주얼하고 직접적인** 커뮤니케이션 톤 선호
- 모든 노트와 답변은 **한국어**로 작성
- 파일을 만들고 나면 **채팅 응답에서 핵심 포인트를 짧게 요약**해줄 것 — 특히 답변한 내용의 요지, 정정한 수치, 처리 방식에 대한 판단(왜 이 메모를 본문으로 넣었는지 등)
- 응답은 간결하게. 파일이 곧 결과물이므로 채팅에서 내용을 반복하지 말 것

---

## 12. 이 프롬프트 사용법

새 대화를 시작할 때 이 문서 전체를 붙여넣은 뒤, 이어서 그날 강의의 이미지/텍스트를 업로드하면 됩니다. Claude는 위 규칙에 따라 자동으로 새 파일 생성 여부를 판단하고, 대괄호 메모와 질문에 답변을 채워 넣어 정리합니다.

**첫 세션에서 Claude가 해야 할 일**: 10장의 "첫 세션에서 확인할 것"을 사용자에게 간단히 물어보고, 답을 받은 뒤 `algorithm/01_...`부터 작업을 시작합니다.