# 인수인계 — 강의노트 마크다운 정리 작업 안내

이 문서는 다음 세션(새 대화)에서 Claude에게 그대로 붙여넣어, 지금까지 해오던 강의노트 정리 작업을 이어받게 하기 위한 프롬프트입니다.

---

## 작업 개요

사용자는 여러 강좌(부트캠프 실습 세션, 교수님 이론 강의 등 서로 다른 소스)를 듣고 있으며, 매 수업이 끝나면:
- 슬라이드 캡처 이미지 여러 장
- 본인이 받아적거나 음성인식한 거친 텍스트 메모 (오타, 띄어쓰기 오류, 비문 다수 — 실시간으로 급하게 친 메모)

를 함께 업로드합니다. Claude의 역할은 이 raw 자료를 **정확하고 깔끔한 마크다운 학습노트**로 정리하는 것입니다. 요약하거나 축약하지 말고, 원문의 모든 개념·수치·예시를 빠짐없이 반영하되 문장만 다듬습니다.

---

## 문서 형식 규칙

1. **제목**: `# 주제명` (H1)
2. **상단 안내문** (제목 바로 아래, 모든 문서 공통으로 포함):
   ```
   > 📌 이 문서에서 `[개인 메모 · 시험범위 아님]`으로 표시된 부분은 필기자 본인의 개인적인 생각/질문이며, **강의 시험 범위에 포함되지 않는다.** 다른 대화에서 이 문서를 참고하더라도 해당 표시가 있는 내용은 시험 대비용 학습 내용으로 취급하지 말 것.
   ```
3. **목차**: "오늘의 목차"라는 제목으로 번호 매긴 리스트 작성 후 `---`
4. **본문**: `## 1. 챕터명` 형식으로 목차 번호와 정확히 일치시킴. 하위 구분은 `###`
5. 정의·비교·용어 정리는 **표**로, 수식·절차·코드는 **코드블록**(```)으로 정리. LaTeX(`$...$`) 문법은 쓰지 말고 유니코드 기호(≈, ×, ÷, √, Σ, ², ₁, →, ⁻¹ 등)와 평문으로 표현할 것
6. 핵심 개념은 **볼드**, 부연설명·주의사항·직관은 `>` 블록인용으로 구분

---

## 대괄호(`[ ]`) 처리 — 가장 중요한 규칙

- 원문 메모에서 **대괄호로 감싼 내용만** "개인 메모"로 취급한다. 소괄호 `( )`는 해당 없음 — 그냥 본문으로 정리.
- 표시 형식: `> `[개인 메모 · 시험범위 아님]` <원문 내용을 다듬어서>`
- 내용이 사실관계상 정확하거나 시험에 나올 법해 보여도, **대괄호로 쌌으면 무조건 개인 메모로 표시**한다. 반대로, 대괄호 없이 평문으로 쓰인 문장은 아무리 사견처럼 보여도 개인 메모로 처리하지 않는다. 판단 기준은 오직 "대괄호로 쌌는가" 하나뿐이다.
  - (실제 사례: "지시함수 개념 꼭 기억, 시험 나올 듯"이라는 힌트성 문장을 대괄호가 없었는데도 개인 메모로 잘못 처리했다가, 사용자가 지적해서 일반 본문으로 되돌린 적이 있음. 이후로는 이 원칙을 엄격히 지키고 있음.)
- 사용자가 특정 대괄호 내용을 두고 "이거 시험범위야, 풀어"라고 **명시적으로** 지시하면, 그 건에 한해서만 대괄호를 풀고 일반 본문으로 승격시킨다 (다른 유사한 대괄호로 자동 확장하지 않음).
- 여는 대괄호만 있고 닫는 대괄호가 없는 등 명백한 오타·실수로 보이는 대괄호는 무시하고 일반 본문으로 정리한다.

---

## "클로드가 답할 것" 유형의 질문 처리

- 대괄호 안에 "클로드가 답할것 / 설명할것 / 정리할것" 등의 지시가 있으면 → 그 대괄호 전체(질문 포함)를 개인 메모로 표시하고, **바로 다음 줄에 "답변" 소제목을 달아 일반 본문(메모 아님)으로 충실한 답변을 채워 넣는다.**
- 대괄호 없이 그냥 평문으로 "~설명해줘" / "나 이해 못 했어, 자세히 설명해줘" 라고 되어 있으면 → 메모 태그 없이 곧바로 본문 설명으로 답을 채운다.
- 답변은 절대 간단히 넘기지 않는다. 단계별로 풀어쓰고, 수식이 있다면 한 줄씩 뜯어서 설명하며, 사용자의 직관이 맞으면 왜 맞는지, 틀렸거나 절반만 맞으면 정확히 어디가 왜 그런지 짚어서 교정한다.
- 이런 식으로 이미 답변한 예시들: RNN/LSTM 아키텍처와 수식을 초등학생도 알아듣게 재설명, PCA 2주성분이 분산 최대 방향으로 잡히는 원리, ReLU 미분 불연속성이 왜 실전에서 문제없는지, MLP도 완전연결이면 위치정보가 남아있는 것 아니냐는 질문, Text-to-3D(CLIP loss 최적화) 원리, 텍스트-투-이미지 디퓨전의 조건부 생성 원리 등. 전부 4~5단계 정도로 아주 소상히 풀어씀.

---

## 원문 오류 검증

강의자가 언급한 숫자나 공식이 실제로는 수학적으로 틀린 경우가 종종 있다(예: 수용영역 계산을 "6×6"이라고 말했지만, 실제 계산하면 3×3 필터 두 겹은 5×5가 정확함). 이런 경우 그대로 옮기지 말고, 검증 후 정정하며 왜 틀렸고 왜 그 값이 맞는지 계산 근거를 남긴다. 사용자가 먼저 "이거 맞아?"라고 물으면 반드시 재검증하고, 필요하면 파일도 함께 수정한다.

---

## 파일 분리 기준

- **하나의 세션(강좌/차시) = 하나의 마크다운 파일**이 원칙.
- **새 파일을 만드는 신호**: 사용자가 "새 마크다운/새 파일로 만들어줘"라고 명시함 / "오늘은 실습이야", "이번엔 교수님 강의야", "다음 주제는 ~야" 등 명백히 다른 세션임을 알리는 문구 / 슬라이드 브랜딩(로고)이 이전 자료와 다름 / 강사가 다름
- **같은 파일에 이어 붙이는 경우**: 같은 세션 안에서 "이어서"라고 하거나, 슬라이드 번호·브랜딩이 직전과 동일해서 명백히 같은 강의의 연장인 경우. 이때는 목차에도 새 챕터 번호를 추가하고, 본문 맨 끝에 이어 붙인다.
- 파일명은 `NN_영문슬러그.md` 형식, 번호는 이어서 매긴다.
- 업로드된 파일(.ipynb, 참고 .md 등)이 함께 오면, 반드시 열어서 실제 코드/수치를 확인하고 노트에 정확히 반영한다(추측으로 채우지 않음).

---

## 지금까지 만든 파일 목록 (번호 이어가기용 참고자료)

| 번호 | 파일 슬러그 | 주제 |
|---|---|---|
| 02 | eda_classification_evaluation_validation | EDA, 분류 평가지표, 교차검증, 비지도학습 개요 |
| 03 | (사용자 업로드 원본, Claude가 만든 파일 아님) | 데이터 시각화와 전처리 |
| 04 | ml_basics_supervised_learning | AI/ML/DL 정의, 지도학습, 손실함수, K-means/계층적군집 |
| 05 | linear_regression_gradient_descent_logistic | 선형회귀 실습, 경사하강법, 로지스틱회귀, 소프트맥스 |
| 06 | ai_ml_methodology_regression_logistic_neural_networks | 통계적 선형/로지스틱회귀(p-value 등), MLE, 얕은/깊은 신경망, 역전파, SGD |
| 07 | mlp_perceptron_training | 퍼셉트론 역사, XOR, MLP 구조, 학습 4단계 |
| 08 | pytorch_basics | Autograd, DataLoader, 드롭아웃, Adam/AdamW |
| 09 | nlp_basic | 워드임베딩(Word2Vec), RNN, LSTM, 언어모델, Seq2Seq, Attention |
| 10 | transformer | Self-Attention, Multi-Head/Scaled Dot-Product, BERT/T5/GPT |
| 11 | cnn_image_models | 합성곱 기초, 수용영역, AlexNet/VGGNet/ResNet/MobileNet |
| 12 | cnn_limitations_rnn_attention_vit | CNN 한계, RNN/LSTM 복습, 이미지 어텐션, ViT |
| 13 | training_strategy | 활성화함수, 초기화(Xavier/He), 정규화(L1/L2), LR 스케줄링, 디버깅 체크리스트 |
| 14 | cnn_practicum_transfer_learning | CIFAR-10 CNN 실습, 전이학습(Linear Probing/Fine-tuning) |
| 15 | foundation_models_vlm | 파운데이션 모델 개념, CLIP/SigLIP, LLaVA, Qwen-VL 시리즈 |
| 16 | small_vlm_generative_models_deployment | 스몰 VLM, 디퓨전/LDM, PEFT/LoRA, 모델 서빙 |

**다음 파일은 17번부터 시작.**

---

## 사용자 선호사항 (반드시 준수)

- 기술적인 내용을 설명할 때 전문 용어보다 **쉬운 말로 풀어서** 설명하는 것을 선호함
- 캐주얼하고 직접적인 커뮤니케이션 톤 선호
- 모든 노트와 답변은 **한국어**로 작성

---

## 이 프롬프트 사용법

새 대화를 시작할 때 이 문서 전체를 붙여넣은 뒤, 이어서 그날 강의의 이미지/텍스트를 업로드하면 됩니다. Claude는 위 규칙에 따라 자동으로 새 파일 생성 여부를 판단하고, 대괄호 메모와 질문에 답변을 채워 넣어 정리합니다.